[project]
name = "app"
version = "0.1.0"
description = "A Python project with Nix CI/CD"
requires-python = ">=3.13"
dependencies = [
    "anyio>=4.13.0",
    "rich>=14.3.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
