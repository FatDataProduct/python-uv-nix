# varlock schema — AI agents get full context, secrets stay safe
# Docs: https://varlock.dev

# @type=enum(development, staging, production)
APP_ENV=development

# @sensitive @required @type=string
# @description Application secret key
SECRET_KEY=

# @type=integer(min=1, max=65535)
PORT=8000

# @type=enum(DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# @sensitive @type=url
# @description Database connection string
# DATABASE_URL=
